buildscript {

    repositories {
        google()
        jcenter()
    }
    dependencies {
        // Android build
        classpath Build.androidGradle
        // Kotlin
        classpath Build.kotlin
    }
}

allprojects {
    repositories {
        google()
        jcenter()
    }
}